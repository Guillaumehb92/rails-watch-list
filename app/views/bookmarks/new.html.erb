  <h1> New bookmark title </h1>

  <%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.input :photo, as :file %>
  <%= f.button :submit %>
<% end %>
